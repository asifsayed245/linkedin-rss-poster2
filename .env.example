# LinkedIn RSS Poster Configuration

# Web Server Port (default: 3000)
PORT=3001

# Hugging Face API Token (get free one at https://huggingface.co/settings/tokens)
# Free tier: 30,000 requests/month
# Optional - app works without it using fallback generation
# ⚠️ IMPORTANT: Replace with your actual token before deployment
HUGGINGFACE_TOKEN=your_huggingface_token_here

# Scheduler settings
SCHEDULE_HOUR=9
SCHEDULE_MINUTE=0
SCHEDULE_TIMEZONE=America/New_York

# Post generation settings
MAX_POSTS_PER_DAY=3
MIN_ARTICLE_LENGTH=200
MAX_ARTICLE_LENGTH=8000
HUGGINGFACE_TOKEN=hf_your_token_here
# AI Model settings (using free Hugging Face models)
SUMMARIZATION_MODEL=facebook/bart-large-cnn
# Alternative models:
# - facebook/bart-large-cnn (good summaries)
# - philschmid/bart-large-cnn-samsum (conversational)
# - sshleifer/distilbart-cnn-12-6 (faster)

# Storage settings
DATABASE_PATH=./data/articles.db
DRAFTS_PATH=./drafts

# RSS Feed settings
MAX_ARTICLES_PER_SOURCE=5
FETCH_TIMEOUT_MS=30000
